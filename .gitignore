__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/
build/
dist/
# real MPD data (never commit the gated dataset)
data/mpd.slice.*.json
# real-data run: downloads, checkpoints, large artifacts (never committed)
.data/

# Taste Atlas app (app/) — React frontend build tooling
node_modules/
app/frontend/node_modules/
app/frontend/test-results/
app/frontend/playwright-report/
app/frontend/.vite/
*.tsbuildinfo
# ...but DO ship the built dashboard bundle (small) so `playlistcont dashboard`
# runs out of the box. Re-included over the global dist/ ignore above.
!/app/frontend/dist/
!/app/frontend/dist/**

# Personal-data artifacts (his taste story etc.) — never commit
scratchpad_artifact/
